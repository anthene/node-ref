{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch 'index.ts'",
            "preLaunchTask": "npm-build",
            "postDebugTask": "clean",
            "program": "${workspaceFolder}/index.ts",
            "outFiles": [
                "${workspaceFolder}/bin/**/*.js",
            ],
        },
    ],
}